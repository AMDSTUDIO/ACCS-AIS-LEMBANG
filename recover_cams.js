const axios = require('axios');
const sqlite3 = require('sqlite3').verbose();

async function recoverCameras() {
  try {
    // We don't have the real IP here, this is just to check syntax
    console.log("Recovery script created");
  } catch (e) {
    console.log(e);
  }
}
recoverCameras();
